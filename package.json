{
  "name": "tiny-git-rev-sync",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "build": "vitest run && tsc -p tsconfig.prod.json",
    "npm-publish": "pnpm run build && npm publish"
  },
  "files": [
    "dist"
  ],
  "repository": "github:lucaslos/vite-plugin-linaria-initial-build-time",
  "author": "Lucas Santos",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": {
    "node": ">=14.6.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@vitest/ui": "^0.15.2",
    "typescript": "^4.7.4",
    "vite": "^2.9.12",
    "vitest": "0.15.2"
  }
}
